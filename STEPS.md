# План разработки NeuronLab

В данном документе описаны этапы реализации проекта от базовой структуры до полноценной многоагентной системы.

## 🔹 Шаг 1 — База (Инфраструктура)
- [x] Настройка окружения Laravel + Docker.
- [x] Конфигурация Redis и PostgreSQL (с расширением pgvector).
- [x] Разработка базового API для взаимодействия с системой:
    - [x] Создание миграций для таблиц `runs` (запуски) и `steps` (шаги).
    - [x] Определение моделей `Run` и `Step` со связями.
    - [x] Реализация эндпоинта для запуска агента (`POST /api/runs`).
    - [x] Реализация эндпоинта для отслеживания статуса и истории (`GET /api/runs/{id}`).
    - [x] Базовая валидация входящих промптов.
- **Результат**: “живое приложение” с готовой инфраструктурой.

## 🔹 Шаг 2 — Базовый агент
- [x] Создание ядра `NeuronAgent`.
- [x] Реализация первого инструмента (например, `search tool`).
- [x] Простейший цикл рассуждений (reasoning loop).
- **Результат**: агент делает несколько шагов.

## 🔹 Шаг 3 — Очереди и асинхронность
- [x] Реализация `StepJob` (обработка шага).
- [x] Перевод reasoning loop в асинхронный режим работы через очереди.
- **Результат**: агент работает через queue.

## 🔹 Шаг 4 — Memory (Память)
- [x] Настройка таблицы embeddings для векторного поиска (pgvector).
- [x] Сохранение истории шагов агента.
- [x] Реализация механизма извлечения контекста (retrieval).
- **Результат**: агент “помнит” свои действия.

## 🔹 Шаг 5 — MCP (Multi-agent)
- [x] Реализация поддержки нескольких типов агентов.
- [x] Маршрутизация задач между агентами.
- [x] Создание `AgentTool` (инструмент для общения агентов).
- **Результат**: агенты взаимодействуют между собой.

## 🔹 Шаг 6 — Reflector
- [x] Добавление этапа анализа ошибок в цикл агента.
- [x] Реализация логики повторных попыток (retry logic).
- [x] Улучшение планов на основе прошлых неудач.
- **Результат**: агент становится “умнее”.

## 🔹 Шаг 7 — Debug & Observability
- [x] Ведение таблицы логирования `agent_steps`.
- [x] Создание интерфейса (UI или API) логов для мониторинга «мыслей» агента.
- [x] Трекинг задержек (latency tracking) на каждом этапе.
- **Результат**: можно дебажить reasoning.

## 🔹 Шаг 8 — Оптимизация
- [x] Ограничение максимального количества шагов (защита от зацикливания).
- [x] Кеширование частотных запросов.
- [x] Уменьшение количества вызовов LLM.
- **Результат**: production-like система.

## 🔹 Шаг 9 — Laravel AI SDK & MCP
- [x] **Интеграция AI SDK**:
    - [x] Установка и настройка провайдеров (OpenAI, Anthropic или др.).
    - [x] Рефакторинг `LLMServiceInterface` на использование фасада `AI`.
    - [x] Настройка чат-моделей и параметров генерации в `config/ai.php`.
- [x] **Внедрение MCP Tools**:
    - [x] Перенос существующих инструментов (`SearchTool`, `AgentTool`) в формат AI SDK Tools.
    - [x] Реализация динамического подключения инструментов через `agent($instructions, $messages, $tools)`.
- [x] **Работа с Prompts**:
    - [x] Вынос системных и пользовательских промптов в `app/Mcp/Prompts` в соответствии с документацией Laravel 13 MCP (через `make:mcp-prompt`).
    - [x] Реализация объектно-ориентированного подхода и тестирование промптов.
- [x] **Внедрение Resources**:
    - [x] Реализация AI Resources для предоставления контекста (например, из БД или файлов).
    - [x] Интеграция ресурсов в процесс генерации ответа.
- [ ] **MCP Server (опционально)**:
    - [ ] Настройка проекта как MCP-сервера для предоставления инструментов внешним клиентам.
- **Результат**: Система использует стандартные механизмы Laravel 13 для работы с AI и MCP.
